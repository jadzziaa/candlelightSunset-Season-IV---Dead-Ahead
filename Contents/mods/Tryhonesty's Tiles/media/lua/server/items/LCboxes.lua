local LibertySuburbDistributions = {

    all = {
        LCboxes = {
            rolls = 2,
            items = {
                "Base.CannedTomato", 100,
		"Base.CannedRedRadish", 75,
		"Base.CannedPotato", 50,
		"Base.CannedLeek", 75,
		"Base.CannedEggplant", 75,
		"Base.CannedCarrots", 75,
                   }
            },
    }
}

table.insert(Distributions, 2, LibertySuburbDistributions);