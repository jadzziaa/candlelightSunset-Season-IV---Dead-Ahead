local LibertySuburbDistributions = {

    all = {
        SprunkCrate = {
            rolls = 2,
            items = {
                "Base.Pop", 100,
		"Base.Pop", 75,
		"Base.Pop", 50,
		"Base.Pop", 75,
		"Base.Pop", 75,
		"Base.Pop", 75,
                   }
            },
    }
}

table.insert(Distributions, 2, LibertySuburbDistributions);