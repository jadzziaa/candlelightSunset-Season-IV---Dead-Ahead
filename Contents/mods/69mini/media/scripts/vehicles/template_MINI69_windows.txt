module Base
{

	model 69miniWindowfl
	{
		mesh = vehicles/Vehicles_69mini_Body|mini_window_fl,
		shader = vehicle,
		static = FALSE,
		scale = 0.1,
	}

	model 69miniWindowfr
	{
		mesh = vehicles/Vehicles_69mini_Body|mini_window_fr,
		shader = vehicle,
		static = FALSE,
		scale = 0.1,
	}

	model 69miniWindowrl
	{
		mesh = vehicles/Vehicles_69mini_Body|mini_window_rl,
		shader = vehicle,
		scale = 0.1,
	}

	model 69miniWindowrr
	{
		mesh = vehicles/Vehicles_69mini_Body|mini_window_rr,
		shader = vehicle,
		scale = 0.1,
	}

	template vehicle MINI69Windows
	{
		part WindowFrontLeft
		{
			model windowFL
			{
				file = 69miniWindowfl,
			}

			anim ClosedToOpen
			{
				anim = window_fl_opening,
				animate = FALSE,
			}

			window
			{
				openable = true,
			}

			parent = DoorFrontLeft,
			area = SeatFrontLeft,
			itemType = Base.69miniSideWindowFront,
			table install
			{
				requireInstalled = DoorFrontLeft,
			}
			table uninstall
				{
				requireUninstalled = MINI69DoorFrontLeftArmor,
				}
		}

		part WindowFrontRight
		{
			model windowFR
			{
				file = 69miniWindowfr,
			}

			anim ClosedToOpen
			{
				anim = window_fr_opening,
				animate = FALSE,
			}

			window
			{
				openable = true,
			}

			parent = DoorFrontRight,
			area = SeatFrontRight,
			itemType = Base.69miniSideWindowFront,
			table install
			{
				requireInstalled = DoorFrontRight,
			}
			table uninstall
				{
				requireUninstalled = MINI69DoorFrontRightArmor,
				}
		}

		part WindowRearLeft
		{
			model windowRL
			{
				file = 69miniWindowrl,
			}

			window
			{
				openable = false,
			}

			area = SeatRearLeft,
			itemType = Base.69miniSideWindowRear,

			table uninstall
				{
				requireUninstalled = MINI69WindowRearLeftArmor,
				}
		}

		part WindowRearRight
		{
			model windowRR
			{
				file = 69miniWindowrr,
			}

			window
			{
				openable = false,
			}

			area = SeatRearRight,
			itemType = Base.69miniSideWindowRear,

			table uninstall
				{
				requireUninstalled = MINI69WindowRearRightArmor,
				}
		}

		part Window*
		{
			category = door,
			mechanicRequireKey = true,
			
			table install
			{
				items
				{
					1
					{
						type = Base.Screwdriver,
						count = 1,
						keep = true,
						equip = primary,
					}
				}
				time = 500,
				skills = Mechanics:3,
				recipes = Intermediate Mechanics,
				test = Vehicles.InstallTest.Default,
				complete = Vehicles.InstallComplete.Window,
			}
			table uninstall
			{
				items
				{
					1
					{
						type = Base.Screwdriver,
						count = 1,
						keep = true,
						equip = primary,
					}
				}
				time = 500,
				skills = Mechanics:3,
				recipes = Intermediate Mechanics,
				test = Vehicles.UninstallTest.Default,
				complete = Vehicles.UninstallComplete.Default,
			}
			lua
			{
				create = Vehicles.Create.Window,
				init = Vehicles.Init.Window,
			}
		}
	}
}

