module Base
{

	model 89trooperWindowfl
	{
		mesh = vehicles/Vehicles_89trooper_Body|trooper_window_fl,
		shader = vehicle,
		static = FALSE,
		scale = 0.1,
	}

	model 89trooperWindowfr
	{
		mesh = vehicles/Vehicles_89trooper_Body|trooper_window_fr,
		shader = vehicle,
		static = FALSE,
		scale = 0.1,
	}

	model 89trooperWindowrl
	{
		mesh = vehicles/Vehicles_89trooper_Body|trooper_window_rl,
		shader = vehicle,
		static = FALSE,
		scale = 0.1,
	}

	model 89trooperWindowrr
	{
		mesh = vehicles/Vehicles_89trooper_Body|trooper_window_rr,
		shader = vehicle,
		static = FALSE,
		scale = 0.1,
	}

	model 89trooperWindowbl
	{
		mesh = vehicles/Vehicles_89trooper_Body|trooper_window_bl,
		shader = vehicle,
		scale = 0.1,
	}

	model 89trooperWindowbr
	{
		mesh = vehicles/Vehicles_89trooper_Body|trooper_window_br,
		shader = vehicle,
		scale = 0.1,
	}

	template vehicle ISZ89Windows
	{
		part WindowFrontLeft
		{
			model windowFL
			{
				file = 89trooperWindowfl,
			}

			anim ClosedToOpen
			{
				anim = window_fl_opening,
				animate = FALSE,
			}

			parent = DoorFrontLeft,
			area = SeatFrontLeft,
			itemType = Base.89trooperSideWindow,
			table install
			{
				requireInstalled = DoorFrontLeft,
			}
			table uninstall
				{
				requireUninstalled = ISZ89DoorFrontLeftArmor,
				}
		}

		part WindowFrontRight
		{
			model windowFR
			{
				file = 89trooperWindowfr,
			}

			anim ClosedToOpen
			{
				anim = window_fr_opening,
				animate = FALSE,
			}

			parent = DoorFrontRight,
			area = SeatFrontRight,
			itemType = Base.89trooperSideWindow,
			table install
			{
				requireInstalled = DoorFrontRight,
			}
			table uninstall
				{
				requireUninstalled = ISZ89DoorFrontRightArmor,
				}
		}

		part WindowRearLeft
		{
			model windowRL
			{
				file = 89trooperWindowrl,
			}

			anim ClosedToOpen
			{
				anim = window_rl_opening,
				animate = FALSE,
			}

			parent = DoorRearLeft,
			area = SeatRearLeft,
			itemType = Base.89trooperSideWindow,
			table install
			{
				requireInstalled = DoorRearLeft,
			}
			table uninstall
				{
				requireUninstalled = ISZ89DoorRearLeftArmor,
				}
		}

		part WindowRearRight
		{
			model windowRR
			{
				file = 89trooperWindowrr,
			}

			anim ClosedToOpen
			{
				anim = window_rr_opening,
				animate = FALSE,
			}

			parent = DoorRearRight,
			area = SeatRearRight,
			itemType = Base.89trooperSideWindow,
			table install
			{
				requireInstalled = DoorRearRight,
			}
			table uninstall
				{
				requireUninstalled = ISZ89DoorRearRightArmor,
				}
		}

		part WindowBackLeft
		{
			model windowBL
			{
				file = 89trooperWindowbl,
			}

			area = SeatRearLeft,
			itemType = Base.89trooperSideWindow,
			table uninstall
				{
				requireUninstalled = ISZ89WindowBackLeftArmor,
				}
		}

		part WindowBackRight
		{
			model windowBR
			{
				file = 89trooperWindowbr,
			}

			area = SeatRearRight,
			itemType = Base.89trooperSideWindow,

			table uninstall
				{
				requireUninstalled = ISZ89WindowBackRightArmor,
				}
		}

		part Window*
		{
			category = door,
			mechanicRequireKey = true,
			window
			{
				openable = true,
			}
			table install
			{
				items
				{
					1
					{
						type = Base.Screwdriver,
						count = 1,
						keep = true,
						equip = primary,
					}
				}
				time = 500,
				skills = Mechanics:3,
				recipes = Intermediate Mechanics,
				test = Vehicles.InstallTest.Default,
				complete = Vehicles.InstallComplete.Window,
			}
			table uninstall
			{
				items
				{
					1
					{
						type = Base.Screwdriver,
						count = 1,
						keep = true,
						equip = primary,
					}
				}
				time = 500,
				skills = Mechanics:3,
				recipes = Intermediate Mechanics,
				test = Vehicles.UninstallTest.Default,
				complete = Vehicles.UninstallComplete.Default,
			}
			lua
			{
				create = Vehicles.Create.Window,
				init = Vehicles.Init.Window,
			}
		}
	}
}

